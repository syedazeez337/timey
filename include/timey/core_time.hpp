#pragma once

namespace timey::core {

// Placeholder for upcoming conversion APIs.
// We keep the library compiling before we add real code.
struct version {
    static constexpr int major = 0;
    static constexpr int minor = 0;
    static constexpr int patch = 0;
};

} // namespace timey::core